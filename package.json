{
  "name": "Connect4",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://tako-chan0511.github.io/Connect4/",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "build:preview": "vue-tsc -b && vite build --base=/",
    "preview": "npm run build:preview && vite preview --port=4173",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@vitejs/plugin-vue": "^5.2.3",
    "@vue/tsconfig": "^0.7.0",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vue-tsc": "^2.2.8"
  }
}
